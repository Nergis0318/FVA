{
  "servers": {
    "fva": {
      "type": "stdio",
      "command": "${env:LOCALAPPDATA}\\Programs\\fva\\bin\\fva.exe",
      "args": ["--path", "${workspaceFolder}"],
      "env": {
        "RUST_LOG": "info"
      }
    }
  }
}